package unihx._internal.editor;
import unityengine.*;
import unityeditor.*;

@:meta(UnityEditor.InitializeOnLoad)
@:nativeGen @:keep class ExampleProjectWindowExt
{
	// static function __init__()
	// {
	// 	unityeditor.EditorApplication.projectWindowItemOnGUI += Draw;
	// }

	// static function Draw(guid:String, rect:Rect):Void
	// {
	// 	var path = AssetDatabase.GUIDToAssetPath(guid);

	// 	rect.x += 15;
	// 	rect.width -= 15;
	// 	rect.y++;

	// 	var elts = path.split('/');
	// 	GUI.Label(rect, elts[elts.length - 1], EditorStyles.label);
	// }
}
