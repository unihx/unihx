package unihx.inspector;

/**
	A special field that represents a directory path anywhere in the computer.

	The GUI will show a button, which when clicked will call a directory selection dialog
**/
typedef DirPath = String;

