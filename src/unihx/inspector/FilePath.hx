package unihx.inspector;

/**
	A special field that represents a file path anywhere in the computer.

	The GUI will show a button, which when clicked will call a file selection dialog
	Options:
		- filter: sets the extension filter
**/
typedef FilePath = String;
